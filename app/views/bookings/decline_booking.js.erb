function refreshModalDeclined(outerHTML) {
  const newModal = document.getElementById('modal-content');
  newModal.outerHTML = outerHTML;
}

refreshModalDeclined('<%= j render "manage_booking", plane: @plane %>');
