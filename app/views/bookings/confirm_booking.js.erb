function refreshModal(outerHTML) {
  const newModal = document.getElementById('modal-content');
  newModal.outerHTML = outerHTML;
}

refreshModal('<%= j render "bookings/manage_booking", plane: @plane %>');
