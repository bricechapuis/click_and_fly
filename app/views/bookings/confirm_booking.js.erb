function refreshModalConfirmed(outerHTML) {
  const newModal = document.getElementById('modal-content');
  newModal.outerHTML = outerHTML;
}

refreshModalConfirmed('<%= j render "manage_booking", plane: @plane %>');
